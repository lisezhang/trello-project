# trello-project

Application web de type Trello (Kanban) avec carte interactive Leaflet/OpenStreetMap.

## Lancer en local

Les modules ES6 nécessitent un serveur HTTP. Lancer avec Python :

```bash
python3 -m http.server 8000
```

Puis ouvrir http://localhost:8000 dans le navigateur.
